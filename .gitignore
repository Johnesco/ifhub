# Game projects now live outside ifhub at /c/code/text-games/<game>/
# Registered in games-registry.json (committed) + games-local.json (overrides)

# Per-developer game path overrides (local paths differ per machine)
games-local.json

# Exploration trees (generated by tools/explore.py)
exploration.json

# Python bytecode
__pycache__/

# Native interpreters (built locally via tools/interpreters/build.sh)
tools/interpreters/*.exe
tools/interpreters/build/

# Rez compiler (downloaded binary, ~24MB)
tools/rez/*.exe
tools/rez/cache/
tools/rez/cloak_test/
tools/rez/dist/
tools/rez/*.dump
