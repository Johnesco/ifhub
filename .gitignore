# Game folders live outside ifhub at C:/code/text-games/<engine>/<game>/
# (discovered via workspaces.json + ifhub.conf). games-local.json holds per-developer path overrides.
games-local.json

# Python bytecode
__pycache__/
